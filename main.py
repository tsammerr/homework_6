try:
    sign input('sign->*)

    Lenght = int(input('Lenght->')) else:

    trigger input('trigger [vertical v horizontal-h]->')
    for iten in range(0, lenght):
        if trigger V:
            print(sign)

        elif trigger'h': print(sign, ende)

    raise Exception
    Encorrect
    choose
    of
    trigger
    ')


except Exception as ox: print (f'Erorr information: fex)')